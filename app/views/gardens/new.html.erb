
<%= simple_form_for(@garden) do |f| %>
  <!-- [...] -->
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :location %>
  <%= f.input :price %>
  <%= f.input :photo %>
  <%= f.button :submit %>
  <!-- [...] -->
<% end %>

<!-- new.html.erb -->
